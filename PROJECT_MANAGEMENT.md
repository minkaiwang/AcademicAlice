# 爱弥斯项目管理

> 当前项目状态与验收入口。架构和历史决策见 `PROGRESS.md`，工程流水见
> `CHANGE_HISTORY.md`，后续计划见 `ROADMAP.md`，原始问题证据与修复对照见
> `AUDIT_REPORT_2026-07-26.md`。

## 1. 当前基线

| 项目 | 当前值 |
|---|---|
| 基线日期 | 2026-07-26 |
| 仓库 / 分支 | `minkaiwang/AemeathDeskPet` / `codex/lts1.0.5pre2-stabilization` |
| 起始提交 | `418a4c1` |
| 对外版本 | `LTS1.0.5pre2`（稳定性预发布候选） |
| 当前阶段 | 深度稳定化与版本收口已完成；草稿 PR #1 已建立且远程 CI 通过，源代码预发布已获授权 |
| 代码发布状态 | `LTS1.0.5pre2` 源代码预发布；草稿 PR 尚未合并，未上传 ZIP / EXE 等二进制附件 |
| 正式二进制发行 | **仍阻断**：受限美术 / 音频的再分发授权与内嵌元宝服务许可证正文尚需维护者提供 |

### 当前结论

- 初始审计确认的数据丢失、无校验更新、发行包污染、明文凭据、正常退出
  强杀、离线资源、服务认证、安装下载与冻结服务等高风险缺陷均已修复。
- 已建立项目级 pytest、Windows GitHub Actions、构建前置检查、确定性发行
  清单、依赖审计和真实浏览器回归；Python 3.11–3.13 远程矩阵已通过。
- 工作台主数据现在写入用户共享目录的版本化 JSON，并有原子写入、轮换
  备份、损坏恢复与旧浏览器缓存迁移。
- 仍不能把自动测试等同于真实账号、音频设备和长时间真人使用。AI、音乐、
  Playwright 登录、麦克风 / GSVmove 需要维护者在自己的账号和硬件上验收。

## 2. 架构与数据地图

| 域 | 主要入口 | 当前职责 |
|---|---|---|
| Windows 启动 | `lib/core/qt_desktop_pet.py`、`启动程序.bat` | 特殊子进程分流、依赖错误、主应用启动 |
| 应用编排 | `lib/script/main.py` | Qt 生命周期、服务装配、优雅退出与超时看门狗 |
| 事件 / 插件 | `lib/core/event/center.py`、`plugin_registry.py` | 7 个管理器、14 个粒子插件的发现与清理 |
| AI / 记忆 | `config/ollama_config.py`、`lib/script/chat/` | 外部 API、Ollama、记忆、视觉输入与错误收敛 |
| 敏感凭据 | `config/secure_secrets.py` | Windows DPAPI、旧明文迁移、清除 |
| 工作台 | `workbench_host.py`、`workbench_storage.py`、`resc/workbench/` | 回环宿主、文件主存储、离线前端与迁移 |
| 元宝中转 | `lib/script/yuanbao_free_api/`、`services/yuanbao-free-api/` | 带 Bearer 鉴权的回环服务、登录状态与冻结子模式 |
| 音乐 / 语音 | `lib/script/music/`、`gsvmove/` 等 | 多源音乐、播放、STT / TTS；外部服务按开关启用 |
| 安装 / 构建 | `install/`、`vendor/` | 安全下载、固定资源哈希、PyInstaller onedir |
| 发布 | `scripts/package_*.py`、`RELEASING.md` | 干净树门禁、确定性 ZIP、manifest、SHA256 |
| 用户共享根 | `config/shared_storage_paths.py` | 显式环境变量 → 已存在旧根 → `%LOCALAPPDATA%` |

## 3. 工作看板

状态：`完成` 有代码与验证证据；`部分完成` 已消除直接风险但仍有结构债；
`外部阻断` 需要维护者材料或真人环境；`规划` 尚未进入当前修复范围。

### 发布安全与稳定性

| ID | 事项 | 状态 | 验收摘要 |
|---|---|---|---|
| DATA-001 | 工作台稳定持久化与恢复 | 完成 | 文件 API、schema、原子写、备份恢复、旧缓存迁移、恢复脚本 |
| UPD-001 | 安全更新事务 | 完成 | 明确资产 / manifest、HTTPS、大小与 SHA256、确认、备份、回滚、路径保护 |
| REL-001 | 可控发行清单 | 完成 | 排除构建 / 用户数据 / 备份；拒绝脏树、符号链接、大小变化和大小写碰撞 |
| SEC-001 | 凭据迁出源码 | 完成 | DPAPI、环境变量优先、旧明文迁移 / 清除、敏感文件排除 |
| SEC-002 | 本机服务与工具边界 | 完成 | shell 默认关闭；元宝 Bearer 鉴权；浏览器工具只接收公网 HTTP(S) |
| LIC-001 | 受限素材再分发授权 | 外部阻断 | 需权利人书面授权或替换素材 |
| LIC-002 | 元宝上游许可证正文 | 外部阻断 | 已保留来源记录，但上游仓库未提供可随包的独立许可证正文 |

### 质量与运行

| ID | 事项 | 状态 | 验收摘要 |
|---|---|---|---|
| QA-001 | pytest + CI | 完成 | 81 passed / 2 skipped；Windows / Python 3.11–3.13 CI；编译、静态检查、审计、打包干跑 |
| WEB-001 | 工作台离线化与浏览器 QA | 完成 | Tailwind / Chart.js / Font Awesome 本地化；桌面、390 px、断网回归 |
| SHUT-001 | 优雅退出 | 完成 | 正常路径不再 `os._exit`；仅超时看门狗保留强制退出 |
| ENV-001 | Python 支持矩阵 | 完成 | 安装、文档、CI、打包统一为 3.11–3.13 |
| LOG-001 | 元宝未登录与服务日志 | 完成 | 空密钥拒绝访问且不启动浏览器；登录状态结构化；后台启动日志可见 |
| BUILD-001 | 冻结版服务与构建前置 | 完成 | 23 模块预检、PyInstaller 6.21.0 构建、冻结服务 200 / 401 与端口释放 |
| DEP-001 | 依赖与供应链 | 部分完成 | 服务 / 开发依赖固定；Vosk 与本地 pyncm 固定哈希；默认移除不安全 musicdl；主依赖尚未生成全平台 lock |
| DOC-001 | 文档与文字完整性 | 完成 | 三类管理文档、审计报告、发布手册、乱码和失真说明已同步 |

### 结构性后续

| ID | 事项 | 状态 | 边界 |
|---|---|---|---|
| CODE-001 | 拆分超大 UI / 音乐模块 | 规划 | 先补行为测试，不做一次性重写 |
| CFG-001 | Python 配置迁移为版本化 schema | 部分完成 | 凭据和工作台已迁出；普通配置仍有 Python 源文件镜像 |
| UX-001 | 真实账号 / 硬件 / 长时运行验收 | 外部阻断 | 需维护者账号、浏览器会话、音频设备与真人判断 |

## 4. 剩余风险

| 风险 | 等级 | 当前控制 |
|---|---|---|
| 受限素材或缺失上游许可证被公开分发 | 高 | `RELEASING.md` 明确阻断；不创建 Release |
| pyncm 1.8.1 已从 PyPI / 上游入口消失，无法进入漏洞库审计 | 中高 | 本地 wheel 逐文件 RECORD 验证、固定 SHA256、最小化封装；后续应替换 |
| 主运行依赖使用范围约束而非完整 lock | 中 | CI / 发布前重新解析并做 `pip-audit`；路线图安排锁定 |
| QQ / 酷狗上游接口变化 | 中 | 直接接口失败时返回空结果，不再默认引入不安全 `musicdl` 依赖链 |
| 外部 AI / 元宝页面 / 音乐接口变动 | 中 | 超时、结构化错误、显式开关；仍需版本发布前在线冒烟 |
| PyInstaller 全量收集 PyQt5 导致包体较大 | 低至中 | 功能优先；后续按真实模块清单减包，不能以删 DLL 冒充修复 |

## 5. 变更与发布规则

1. 当前事实优先级：代码与可复现证据 → 本文件 → `PROGRESS.md` ADR →
   历史说明。
2. 正式修改前保留 `.bak`；发行清单会排除备份、日志、用户数据和构建缓存。
3. 数据、更新、凭据、下载、解压和进程结束逻辑必须带失败场景测试。
4. 工作台改动至少检查 1440×900、390×844 与断网；浏览器测试不能代替
   真人内容理解。
5. 发行前必须在干净树执行 `RELEASING.md`，核对许可证、manifest、SHA256
   与冻结 EXE 冒烟。
6. 提交、推送、Tag、Release、公开上传与删除历史均需维护者最终确认。

## 6. 下一步

1. 维护者提供受限素材授权与元宝服务许可依据，或决定替换 / 不随包分发。
2. 在真实账号和硬件上执行 AI、QQ / 网易 / 酷狗、元宝扫码、麦克风、
   GSVmove 和 30–60 分钟退出回归。
3. 审查草稿 PR #1 的全量 diff 与远程 CI，确认后合并；源代码预发布之外仍不上传二进制附件。
4. 下一开发周期优先做运行依赖 lock 与 pyncm 替代，再拆分超大模块。
