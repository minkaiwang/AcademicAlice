# 爱弥斯未来规划

> 路线从 2026-07-26 稳定化基线继续推进。已完成事项的证据见
> `CHANGE_HISTORY.md`；本文件只保留尚需完成或下一阶段最有价值的工作。
> 当前发布候选：`LTS1.0.5pre2`，分支
> `codex/lts1.0.5pre2-stabilization`。

## 路线总览

| 阶段 | 目标 | 进入条件 | 完成门槛 |
|---|---|---|---|
| R0 | 发布许可与真人验收 | `LTS1.0.5pre2` 候选代码 | 许可清关、真实账号 / 硬件冒烟、干净树构建 |
| R1 | 依赖可复现与供应链收敛 | R0 决策明确 | 全平台约束、pyncm 替代、SBOM、可复核来源 |
| R2 | 配置与大模块治理 | 行为测试覆盖 | schema 迁移、小步拆分、旧配置回退 |
| P1 | 工作台产品化 | 稳定数据层已完成 | 关键流程、可访问性、恢复演练 |
| P2 | 备份与换机 | P1 字段稳定 | 预览、合并 / 替换、校验与干净机恢复 |
| P3 | 桌宠—工作台联动 | P2 完成 | 本地默认、显式授权、可预览 / 清除 |
| P4 | 发布生态 | R0–R2 基本完成 | 签名、安装器、更新服务、贡献闭环 |

## R0：发布前清关与真人验收

### R0.1 许可

- 获取 `LICENSE-ASSETS` 所涉美术、音频、字体和角色资源的可保存书面授权；
- 若无法取得，建立可公开再分发的替代资源包，默认构建不得夹带受限资源；
- 向元宝内嵌服务上游核实准确许可证、版权声明和允许的再分发范围；
- 每个新增二进制、模型、字体、网页库和数据源同步来源、版本、许可证与哈希。

### R0.2 真人 / 真机矩阵

- Windows 10 与 11 各一次安装、首次启动、重复启动、托盘退出；
- OpenAI 兼容接口和 Ollama 各完成一次对话、流式中断与错误恢复；
- QQ、网易、酷狗各完成搜索 / 播放 / 暂停 / 切歌，记录接口失效而非猜测成功；
- 元宝完成扫码登录、状态恢复、注销和无密钥拒绝；
- 麦克风、Vosk、GSVmove 在有 / 无设备和服务关闭时均不阻塞退出；
- 30–60 分钟常驻，确认内存、线程、日志增长和退出时间。

### R0.3 正式构建

- 干净工作树执行 `RELEASING.md` 全清单；
- 使用隔离 Python 3.13 环境安装运行 / 开发依赖；
- 全量 PyInstaller 构建后检查冻结 EXE、内嵌服务、许可证和资源；
- 对最终 ZIP 复核 manifest、SHA256、文件清单和解压后启动；
- 停在 Tag、上传和 Release 最终确认前。

## R1：依赖与供应链

### R1.1 运行依赖锁定

- 以 Python 3.11、3.12、3.13 / Windows x64 生成可审计 constraints；
- 区分直接依赖、传递依赖、构建依赖和可选外部能力；
- 在 CI 做 `pip check`、`pip-audit` 和过期依赖报告；
- 对无法进入公开漏洞库的组件使用来源 commit、wheel RECORD、SHA256 和人工审计。

### R1.2 替换 pyncm

- 评估维护中的网易云接口库或只保留项目内最小只读 API；
- 对搜索、详情、播放 URL、登录态分别写契约测试；
- 明确服务条款与接口稳定性，不复制未知许可代码；
- 完成替换后移除 `vendor/pyncm-1.8.1-*.whl` 和兼容分支。

### R1.3 构建瘦身

- 用真实导入证据替代 `collect_all("PyQt5")` 的全量 Qt 收集；
- 保留桌宠、音频、网络、Playwright 和服务所需模块；
- 每次删除 DLL / 插件后执行冻结版桌宠、服务、音乐和图像回归；
- 目标是减小包体和警告，不以“能生成 EXE”作为唯一验收。

## R2：配置与代码治理

### R2.1 配置 schema

- 普通设置迁移到带 `schema_version` 的 JSON / TOML；
- 敏感值继续由 DPAPI 管理，运行缓存与配置分目录；
- 每次迁移先备份，失败回退旧配置并给出可行动错误；
- 保留 Python 配置只读兼容层，稳定两个版本后再删除写回逻辑。

### R2.2 模块拆分

优先顺序：

1. `ai_settings_panel.py`：字段 schema、视图、持久化、运行时应用分离；
2. `cloudmusic/_mixin_login.py`：按登录策略拆分；
3. QQ / 酷狗：统一 provider 协议与结构化错误；
4. `install_deps.py`：解释器、pip、依赖、模型资源、启动分离。

每一步先固定行为测试，只允许单职责迁移，不做一次性重写。

## P1：科研工作台产品化

- 逐页验证项目、论文、投稿、报销、健康、导师沟通和复盘的新增 / 编辑 /
  删除闭环；
- 为关键删除和批量导入增加预览 / 撤销；
- 检查键盘焦点、对比度、缩放、长文本和移动端安全区；
- 给 `state.json` schema 写显式迁移测试和匿名演示数据；
- 增加“最近备份 / 恢复”可视入口，而不只依赖命令行脚本。

## P2：备份与换机

- 导出包包含 schema、应用版本、生成时间、文件数量与校验摘要；
- 导入前展示新增、修改、冲突和无法识别字段；
- 支持“合并 / 替换 / 取消”，替换前自动备份；
- 明确工作台、普通配置、DPAPI 凭据、模型、浏览器登录态和附件中哪些可迁移；
- 在干净 Windows 用户上完成“导出 → 换机 → 导入 → 逐项核对”。

## P3：桌宠与工作台联动

- 默认只读取本地摘要，不把论文标题、笔记或个人数据发送到外部模型；
- 外部 AI 使用工作台上下文前必须显式启用并预览字段；
- 摘要可选择、可撤销、可清除，并记录最近一次使用范围；
- 提醒与建议不直接修改工作台，写操作回到工作台确认；
- 区分相关性建议与因果 / 成效宣称，避免过度承诺。

## P4：发布生态

- 统一版本命名与迁移策略；
- 增加代码签名、安装器升级 / 卸载、旧版本回滚与崩溃日志导出；
- 生成 SBOM、第三方 notice、资源许可 manifest；
- 建立 Release 候选渠道，先小范围验证再公开；
- 发布后保留可撤回附件、已知问题和热修复记录。

## 暂不优先

- 新增更多 AI 或音乐提供商；
- 云端多人协作；
- 大规模视觉重做；
- 工作台与 SQLite 双向同步；
- 没有真实用户证据的新复杂桌宠对象。

这些功能会扩大依赖与回归面，当前价值低于许可、依赖、恢复和真实运行闭环。
