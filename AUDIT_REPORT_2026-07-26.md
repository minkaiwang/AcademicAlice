# 爱弥斯工作区深度审计报告

> 审计日期：2026-07-26
> 范围：本地仓库、Git 状态、Python 代码、安装 / 发布链、科研工作台、现有运行日志与项目文档。
> 状态：下文保留初始审计快照；同日修复结果见本节。未创建 tag、Release 或远程提交。

## 0. 修复结果（同日更新）

初始审计中的可复现高风险代码缺陷已经修复；正式公开发行仍因素材再分发
授权和元宝内嵌服务许可证正文缺失而阻断。这两个事项需要维护者提供权利
材料或替换组件，不能用代码改动自行推定。

| 初始问题 | 当前状态 | 主要证据 |
|---|---|---|
| DATA-001 随机端口分割数据 | 已修复 | 共享根 `workbench/state.json`、文件 API、原子写、备份恢复、旧缓存迁移 |
| UPD-001 无确认 / 校验覆盖 | 已修复 | manifest、明确资产、HTTPS、版本排序、大小 / SHA256、确认、备份、回滚与安全解压 |
| REL-001 打包混入 `build/` | 已修复 | 源码包 868 文件 / 42,760,365 bytes，绿色清单 896 文件 / 181,951,603 bytes；两者干跑通过 |
| SEC-001 明文凭据写源码 | 已修复 | Windows DPAPI、环境变量优先、旧明文迁移 / 清除 |
| QA-001 无测试 / CI | 已修复 | 18 个测试文件；Windows / Python 3.11–3.13 CI |
| WEB-001 外部 CDN | 已修复 | Tailwind、Chart.js、Font Awesome 本地化；桌面 / 移动 / 断网 Chromium 通过 |
| SHUT-001 正常退出强杀 | 已修复 | 正常退出走 Qt finalize；`os._exit` 仅为超时看门狗 |
| DEP-001 浮动 / 不安全依赖 | 直接风险已修复 | 服务 / 开发依赖固定，资源哈希固定；移除不安全 `musicdl` 默认链；主运行依赖 lock 仍属后续 |
| LOG-001 元宝未登录刷异常 | 已修复 | 空密钥 401 且不启动浏览器，结构化状态与后台启动日志 |
| 冻结版元宝服务不监听 | 已修复 | 最终 EXE `/openapi.json` 200，未授权 `/fsv/status` 401，结束后端口释放 |
| LIC-001 / LIC-002 | 外部阻断 | 需受限素材授权和元宝上游许可证正文 |

### 最终自动验证

- `pytest`：**81 passed，2 skipped，21 subtests passed**；两项 skip 均因当前
  Windows 账户无创建符号链接权限，对应路径拒绝逻辑仍有非符号链接测试；
- `compileall`：`config`、`lib`、`install`、`scripts`、`services`、`tests`
  通过；
- Ruff 关键正确性规则 `E9,F63,F7,F82,F841`：通过；
- 品牌检查：通过；
- `pip check`：通过；
- 内嵌服务依赖审计：无已知漏洞；
- 开发依赖审计：无已知漏洞；
- 运行依赖审计：无已知漏洞；`pyncm 1.8.1` 因 PyPI 项目不可查询被明确
  跳过，其本地 wheel 由 RECORD 与固定 SHA256 验证；
- PyInstaller 6.21.0 完整隔离构建：成功，无 `ERROR`；最终目录
  **3,513 个文件 / 670,962,040 bytes**，EXE **17,839,711 bytes**；
- 冻结归档包含 `playwright.async_api`，不包含已移除的 `musicdl`；
- 工作台真实 Chromium：1440×900、390×844、移动菜单与断网场景通过。

本轮依赖审计调用 `pip-audit` 时，本机火绒对其 Apache-2.0 依赖
`cyclonedx-python-lib` 的 `vulnerability.cpython-313.pyc` 触发
`HackTool/VulnScan` 启发式告警并处理该可再生成缓存。核对后 Git 跟踪文件
无删除，源码包元数据与依赖关系正常；未关闭防护或添加白名单，也未再次运行
该扫描器。此记录只描述本机审计环境，不把启发式判断反向写成项目安全结论。

### 自动化无法替代的验收

- 真实 OpenAI / Ollama、QQ / 网易 / 酷狗账号与接口；
- 元宝扫码登录和登录态长期恢复；
- 麦克风、声卡、Vosk、GSVmove 的实际硬件链；
- 30–60 分钟常驻、真人交互理解和视觉主观质量；
- 受限素材及第三方服务的最终法律授权。

以下第 1–8 节为修复前的原始审计证据，保留用于追溯，不再代表当前代码状态。

## 1. 结论

项目已有完整桌宠主链、动态管理器、音乐 / 语音 / AI 能力和体量较大的科研工作台，不是空壳原型。当前最大问题也不是语法错误，而是以下四条工程闭环尚未成立：

1. **数据闭环**：工作台以随机端口启动，却把主数据放在按端口隔离的 `localStorage`。
2. **更新闭环**：检查更新会直接覆盖文件，没有确认、校验、备份和回滚。
3. **发布闭环**：发行脚本扫描整个工作区，纳入被 Git 忽略的旧 `build/` 产物。
4. **安全 / 合规闭环**：凭据写入受跟踪源码；资源再分发口径与许可证文字存在冲突；第三方许可证随包材料不完整。

因此，建议先完成 `PROJECT_MANAGEMENT.md` 中的 S0 稳定化，再继续新增产品功能。

## 2. 仓库与规模

| 检查项 | 结果 |
|---|---|
| Git 分支 | `main`，与 `origin/main` 对齐 |
| 工作区基线 | 审计开始时干净 |
| 当前提交 | `418a4c1` |
| Git 对象包 | 30.78 MiB |
| 受跟踪文件 | 821 |
| Python 文件 / 行数 | 256 / 46,929 行（含内嵌服务） |
| 最大 Python 模块 | `lib/script/ui/ai_settings_panel.py`，3,386 行 |
| 正式测试 | 仅发现 `services/yuanbao-free-api/test.py` 集成脚本；未发现项目级测试套件 |
| CI | 未发现 `.github/workflows` |

### 代码结构判断

- `qt_desktop_pet.py` 负责解释器 / 冻结环境入口与启动错误提示。
- `main.py` 负责 Qt 生命周期、服务装配、管理器发现和退出流程。
- `plugin_registry.py` 自动扫描 `lib/script/obj-*/manager.py` 与粒子脚本。
- 业务扩展点清楚，但多个模块已超过 500–3,000 行，回归风险依赖人工经验。
- 事件订阅与清理已有统一思想，但大量广泛 `except Exception: pass` 会吞掉诊断信号。

## 3. 已执行验证

| 验证 | 结果 | 说明 |
|---|---|---|
| Python 3.14 `compileall` | 通过 | 只证明语法可编译，不证明依赖齐全 |
| Python 3.13 `compileall` | 通过 | 覆盖 `config`、`lib`、`install`、`scripts` 和内嵌服务 |
| Python 3.14 `pip check` | 通过 | 仅检查已安装包内部一致性；该解释器实际缺 PyQt5 |
| 品牌字符串检查 | 通过 | `scripts/check_brand_strings.py` 未发现禁止字符串 |
| Python 3.13 插件发现 | 通过 | 7 个管理器、14 个粒子插件 |
| Python 3.14 插件发现 | 失败 | 全部因 `No module named 'PyQt5'` 无法导入 |
| 源码包 dry-run | 逻辑执行成功，验收失败 | 832 文件、325.61 MB，错误包含 285.69 MB `build/` |
| 绿色包清单诊断 | 验收失败 | 860 文件、458.35 MB，同样包含 285.69 MB `build/` |
| 工作台桌面视口 | 通过 | Chromium 1440×900，布局与图标正常 |
| 工作台移动视口 | 通过 | Chromium 390×844，折叠菜单正常 |
| 工作台联网控制台 | 通过 | 无 error / warning；仅 1 条浏览器 verbose 表单提示 |
| 工作台断网模拟 | 降级失败 | Font Awesome 与 Chart.js 请求失败，图标消失、移动菜单按钮空白、图表不可用 |

未执行正式 PyInstaller 重打包、真实 AI / 音乐账号登录和长时间真人桌宠回归；这些需要在代码修复后进入受控验收。

## 4. 确认问题

### [P0] DATA-001：工作台数据被随机端口分割

证据：

- `lib/script/workbench_host.py` 使用 `ThreadingHTTPServer(("127.0.0.1", 0), ...)`，`0` 表示每次进程选择随机端口。
- `resc/workbench/research_workbench.html` 的非 Tauri 路径明确把主状态写入 `localStorage`，键为 `phd_master_workspace_merged_v1`。
- 浏览器存储按 origin 隔离，而端口是 origin 的组成部分。

影响：

- 重启桌宠后打开新端口，工作台会像“数据被清空”一样显示空状态。
- 旧数据通常仍留在旧端口的浏览器存储中，但用户没有稳定入口恢复。
- 端口日后偶然复用时，还可能重新出现某次旧快照，造成状态混乱。

建议：

- 不把浏览器 origin 当主数据存储。优先实现稳定文件 / SQLite 存储，由固定本机 API 读写。
- 在迁移完成前至少使用稳定端口并清楚提示冲突，但固定端口只能止血，不能自动找回历史随机端口数据。
- 提供“从旧浏览器备份 JSON 导入”的恢复流程；若要自动恢复，需要设计受控的旧端口扫描 / 用户选择方案。

### [P0] UPD-001：检查更新会立即无校验覆盖

证据：

- 设置页按钮名为“检查更新”，调用 `UpdateManager.check_and_update()`。
- 管理器发现新版本后直接下载第一个以 `.zip` 结尾的资产。
- 随后 `extractall` 并以 `shutil.copy2` 覆盖当前项目树。
- 未发现 SHA256、签名、允许的资产命名、二次确认、事务暂存、备份或回滚。

影响：

- 用户以为只做检查，实际触发大范围本地覆盖。
- Release 中第一个 zip 若是源码包、旧包或错误附件，可能被当作更新包。
- 中断或部分复制会留下混合版本。

建议：立即把按钮降级为只读检查；后续按“检查 → 展示 → 用户确认 → 下载 → 校验 → 备份 → 原子切换 → 健康检查 → 回滚”重建。

### [P0] REL-001：发行包混入旧构建缓存

证据：

- `scripts/package_release.py` 与 `scripts/package_green_release.py` 的排除目录没有 `build`。
- 两者使用 `ROOT.rglob("*")` 扫描整个工作区，而不是以 Git 跟踪清单或白名单为准。
- dry-run 纳入 11 个 `build/deskpet/` 旧品牌构建文件，共 285.69 MB。

影响：

- 发行包体积异常；
- 带入旧品牌二进制与构建交叉引用；
- 本机存在什么临时文件会直接影响正式包，无法复现。

建议：使用显式白名单或 Git manifest；最少补 `build` 排除，并把“不含 build/dist/user/log/secret”写成自动测试。

### [P0] SEC-001：凭据写入受跟踪源码

证据：

- `save_ai_values()` 把 `API_KEY`、`x_uskey` 等值正则写回 `config/ollama_config.py`。
- 该文件受 Git 跟踪，不在 `.gitignore` 保护范围。
- 同一明文内容还镜像到 `C:\AemeathDeskPet\config\ollama_config.py`。

影响：用户在设置页保存后，可能无意中把密钥或登录态提交到 Git；本机共享副本也是明文。

建议：源码只保留模板；敏感值迁移到环境变量、Windows Credential Manager / DPAPI 或独立忽略的 secrets 文件，并提供一次性清理检查。

### [P1] QA-001：测试与 CI 缺失

- 项目文档提到观察 GitHub Actions CI，但仓库没有 `.github/workflows`。
- 现有检查以 `compileall`、dry-run 和手工启动为主，无法保护数据迁移、更新覆盖、包清单、配置合并等高风险路径。

### [P1] WEB-001：本地优先产品仍依赖外部 CDN

- Tailwind 已本地化，但 Chart.js 和 Font Awesome 仍从 jsDelivr / cdnjs 加载。
- 实测断网后两项请求返回失败，图标消失，移动端菜单触发器变成空白，图表能力不可用。

### [P1] SHUT-001：正常退出以 `os._exit` 收尾

- 退出步骤为运行时服务清理 → 退出动画 → `force_quit_application`。
- 最后调用 `os._exit`，跳过正常事件循环返回、`finalize_after_event_loop()`、可视组件清理与 logger cleanup。
- 现有日志也显示正常退出最终进入 `force_quit_application`。

这可能是为解决历史卡死而加的兜底，但当前兜底已成为默认路径，应改成“有超时的最后手段”。

### [P1] DEP-001：安装来源不可复现

- 根依赖使用大量 `>=`，没有 lock / constraints。
- 安装器可下载 `chenwr727/yuanbao-free-api` 的浮动 `main.zip`，没有 commit/ref 与 SHA256 固定。
- `services/README.txt` 声称存在内置 `services/bundles/yuanbao-free-api-main.zip`，当前仓库并无该目录 / 文件。

### [P1] LOG-001：未登录辅助服务持续制造异常日志

现有 `yuanbao_free_api_launcher.log` 中，同一“无法获取请求头，请确保已登录”异常出现 122 组，并伴随大量 ASGI traceback 和 500。应用日志还记录 GSVmove 启动脚本缺失与启动超时。需要把“未登录 / 未安装”建模为正常状态并限流，不应当作高频异常。

### [P2] DOC-001 / CODE-001：文档与维护债

- `PROGRESS.md` 同时承担设计、里程碑、ADR 和逐日修改史，难以快速判断当前优先级。
- 发现 5 处明显乱码注释 / 日志文字。
- `ai_settings_panel.py` 3,386 行；云音乐登录和播放等模块也明显超大。
- 发行脚本与 Release 文档仍使用 `FlyingSnowVelvet-*` 名称，而绿色包使用 `AemeathDeskPet`，版本 / 品牌口径不统一。

## 5. 高度可疑

### Python 3.7 支持声明缺少证据

安装器最低版本仍为 3.7，贡献指南写 3.7–3.13；但当前没有低版本 CI，依赖也未按 Python 版本锁定。现有证据只能支持本机 Python 3.13 运行环境，不能支持 3.7 承诺。

### 更新资产选择可能选错包

代码选择 Release 中第一个 `.zip`，但未检查名称、结构、平台和 manifest。具体会选到哪个包取决于 Release 附件顺序，需结合真实 Release 做只读核对。

### `/` shell 命令能力缺少产品安全边界

命令框以 `/` 开头时直接 `shell=True` 执行本机命令。当前证据显示这是用户显式输入能力，AI 工具调度器不会直接调用它；但仍建议增加首次风险提示、可关闭开关和审计记录。

## 6. 需作者核实

### LIC-001：资源公开分发授权

`LICENSE-ASSETS` 写明资源只可运行、评估或展示，未经权利人书面许可不得再分发；与此同时，仓库公开包含这些资源，README / Release 文档指导打包发送。项目文档提到“原作者允许二创”，但本仓未见可审计的书面授权文件。

在作者提供授权证据前，应把资源发行视为待核实，不根据本报告作法律结论。

### LIC-002：第三方许可证随包

工作台标注基于 MIT 上游，元宝服务 readme 也写 MIT，但仓库只发现根 `LICENSE-CODE` 与 `LICENSE-ASSETS`，未发现对应上游 MIT LICENSE/NOTICE 副本。需要核对原始版权声明并随源码 / 发行包保留。

## 7. 正向发现

- Git 远端、仓库名与 `GITHUB_REPO` 已一致。
- 品牌字符串检查通过。
- `py.ini` 启动脚本明确选择 Python 3.13，可规避系统默认解释器漂移。
- 事件中心、管理器注册、清理入口和分层意图清楚。
- 工作台 Tailwind 已本地化，桌面 / 移动端视觉质量较好。
- 工作台已有 JSON 导出 / 导入与存储异常提示，为修复 DATA-001 提供了迁移基础。
- 用户数据目录、模型和日志已在 `.gitignore` 与普通源码包排除逻辑中考虑。
- 本机服务只监听 `127.0.0.1`，没有直接暴露到局域网。

## 8. 建议验收顺序

1. 先修 DATA-001，并用“写入 → 退出 → 重启 → 读取 → 导出 → 导入”验证。
2. 再修 UPD-001 与 REL-001，建立不会破坏安装的更新 / 发布链。
3. 迁移凭据，补许可材料。
4. 建 CI 和核心测试。
5. 最后处理离线资源、退出流程、日志降噪和模块拆分。
